{
  // tstyche-only config: same compiler options as the package tsconfig, but
  // *.tst.ts files are not excluded, so type tests certify the adapter under
  // the repo's real settings (exactOptionalPropertyTypes etc.), not tstyche's
  // built-in baseline. Mirrors packages/core/tsconfig.tstyche.json.
  "extends": "./tsconfig.json",
  "exclude": []
}
