---
"docs": patch
---

Serve `/llms.txt` (per-page index) and `/llms-full.txt` (the full docs corpus as processed markdown, MDX components compiled away) for agents doing web lookups without the bundled skill installed. Both are statically rendered at build time.
